import { PlaceholderContent } from "@/components/placeholder-content";

export default function SchedulerPage() {
    return <PlaceholderContent title="Scheduler" description="Set schedules for exams, course availability, and other events." />;
}
