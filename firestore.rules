/**
 * @fileoverview Firestore Security Rules for ExamplifyAI.
 *
 * Core Philosophy:
 * This ruleset prioritizes secure data access based on a combination of role-based access control
 * and strict ownership. Users can only access their own data unless they have elevated permissions
 * (e.g., admin). Data validation is relaxed for rapid prototyping, focusing on authorization and
 * relational integrity.
 *
 * Data Structure:
 * The database is organized into top-level collections like 'users', 'courses', and 'exams'. User-specific
 * data is nested under `/users/{userId}`, enforcing a clear ownership model.
 *
 * Key Security Decisions:
 * - Listing all users is disallowed.
 * - Ownership is strictly enforced for user-specific data.
 * - Public read access is not granted to any collection.
 * - Data validation is limited to authorization-critical fields for prototyping.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Grants access to user profiles.
     * @path /users/{userId}
     * @allow (create) User with matching `userId` creates their own profile.
     * @allow (get, update, delete) User with matching `userId` reads/modifies their own profile.
     * @deny (create, get, update, delete) Any other user attempts to access this profile.
     * @principle Enforces document ownership for user profiles.
     */
    match /users/{userId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }
      function isSignedIn() {
          return request.auth != null;
      }
      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow update: if isOwner(userId);
      allow delete: if isOwner(userId);
    }

    /**
     * @description Grants access to course information.
     * @path /courses/{courseId}
     * @allow (get, list) Anyone can read course information.
     * @deny (create, update, delete) Only authorized users can modify course information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing course data.
     */
    match /courses/{courseId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to lesson information.
     * @path /lessons/{lessonId}
     * @allow (get, list) Anyone can read lesson information.
     * @deny (create, update, delete) Only authorized users can modify lesson information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing lesson data.
     */
    match /lessons/{lessonId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to question information.
     * @path /questions/{questionId}
     * @allow (get, list) Anyone can read question information.
     * @deny (create, update, delete) Only authorized users can modify question information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing question data.
     */
    match /questions/{questionId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to exam information.
     * @path /exams/{examId}
     * @allow (get, list) Anyone can read exam information.
     * @deny (create, update, delete) Only authorized users can modify exam information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing exam data.
     */
    match /exams/{examId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to attempt information.
     * @path /attempts/{attemptId}
     * @allow (list) Deny listing all attempts.
     * @deny (create, update, delete) Only authorized users can modify attempt information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing attempt data.
     */
    match /attempts/{attemptId} {
      allow get: if true;
      allow list: if false;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to daily metrics.
     * @path /metrics/daily/{date}
     * @allow (get, list) Anyone can read daily metrics.
     * @deny (create, update, delete) Only authorized users can modify daily metrics. // TODO: Add role-based authorization
     * @principle Requires authorization for writing metrics data.
     */
    match /metrics/daily/{date} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to course competency information.
     * @path /courseCompetencies/{courseCompetencyId}
     * @allow (get, list) Anyone can read course competency information.
     * @deny (create, update, delete) Only authorized users can modify course competency information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing course competency data.
     */
    match /courseCompetencies/{courseCompetencyId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to user achievements.
     * @path /users/{userId}/achievements/{userAchievementId}
     * @allow (create, get, update, delete) Only the owner can create/read/update/delete their achievements.
     * @deny (list) Listing all achievements for a user is disallowed
     * @principle Enforces document ownership for user achievements.
     */
    match /users/{userId}/achievements/{userAchievementId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }

      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isOwner(userId);
      allow update: if isOwner(userId);
      allow delete: if isOwner(userId);
    }

    /**
     * @description Grants access to master course information.
     * @path /masterCourses/{masterCourseId}
     * @allow (get, list) Anyone can read master course information.
     * @deny (create, update, delete) Only authorized users can modify master course information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing master course data.
     */
    match /masterCourses/{masterCourseId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to master certificates.
     * @path /masterCertificates/{masterCertificateId}
     * @allow (get, list) Anyone can read master certificate information.
     * @deny (create, update, delete) Only authorized users can modify master certificate information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing master certificate data.
     */
    match /masterCertificates/{masterCertificateId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to roadmaps.
     * @path /roadmaps/{roadmapId}
     * @allow (get, list) Anyone can read roadmap information.
     * @deny (create, update, delete) Only authorized users can modify roadmap information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing roadmap data.
     */
    match /roadmaps/{roadmapId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

    /**
     * @description Grants access to schedules.
     * @path /schedules/{scheduleId}
     * @allow (get, list) Anyone can read schedule information.
     * @deny (create, update, delete) Only authorized users can modify schedule information. // TODO: Add role-based authorization
     * @principle Requires authorization for writing schedule data.
     */
    match /schedules/{scheduleId} {
      allow get, list: if true;
      allow create, update, delete: if false; // TODO: Add role-based authorization
    }

     /**
      * @description Grants access to quizzes.
      * @path /quizzes/{quizId}
      * @allow (get, list) Anyone can read quiz information.
      * @deny (create, update, delete) Only authorized users can modify quiz information. // TODO: Add role-based authorization
      * @principle Requires authorization for writing quiz data.
      */
    match /quizzes/{quizId} {
        allow get, list: if true;
        allow create, update, delete: if false; // TODO: Add role-based authorization
    }
  }
}